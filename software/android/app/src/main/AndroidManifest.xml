<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skateflair.flair">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FlairActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FlairCreateGroupActivity"
            android:label="@string/title_activity_flair_create_group"></activity>

        <service android:name=".ServiceFlairController" />

        <activity
            android:name=".FlairSettingsActivity"
            android:label="@string/title_activity_flair_settings"
            android:parentActivityName=".FlairActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.skateflair.flair.FlairActivity" />
        </activity>
    </application>

</manifest>
